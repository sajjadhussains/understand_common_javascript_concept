function add(a, b) {
  const total = a + b;
  console.log(total);
  return total;
}

console.log(total);
add(5, 6);

//block scope means === usually any variable that stays within second bracket which can not be excess from outside of the bracket.if you don't use var.because var is hoisted.hoisting means it declares above the variable.
